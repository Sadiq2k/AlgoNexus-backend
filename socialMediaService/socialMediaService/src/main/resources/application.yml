server:
  port: 8082


spring:
  config:
    import: optional:configserver:http://localhost:8085
  data:
    mongodb:
      uri: mongodb://localhost:27017
      database: algo-nexus-SOCIAL-MEDIA
      auto-index-creation: true
      auto-create-indexes: true

  application:
    name: SOCIAL-MEDIA-SERVICE

